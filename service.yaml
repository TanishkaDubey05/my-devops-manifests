apiVersion: v1
kind: Service
metadata:
  name: tanishka-app-service
  namespace: default
spec:
  selector:
    app: tanishka-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
